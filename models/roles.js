module.exports.ROLES = {
    SUPERADMIN: 'superadmin',
    ADMIN: 'admin',
    USER: 'user'
};

// This didn't work as expected

//const ENUM = require('enum');
//module.exports.ROLES = new ENUM({SUPERADMIN: "superadmin", ADMIN : "admin", USER: 'user' });

